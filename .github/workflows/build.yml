name: build
on: [push]
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v2
            - run: |
            echo "Start build"
            mkdir build
            cd build
            echo "End build"
            